version: "3.9"

services:
  rag_api:
    build:
      context: ./rag  
      dockerfile: Dockerfile
    container_name: rag_api
    ports:
      - "8010:8010"
    environment:
      - QDRANT_API_KEY=${QDRANT_API_KEY}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8010/"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - project-network

  streamlit_app:
    build:
      context: ./interface_app  
      dockerfile: Dockerfile
    container_name: streamlit_app
    ports:
      - "8501:8501"
    depends_on:
      rag_api:
        condition: service_healthy
    environment:
      - DATABASE_HOST=${DATABASE_HOST}
      - DATABASE_PASSWORD=${DATABASE_PASSWORD}
      - RAG_API_URL=http://rag_api:8010  # Added this for your Streamlit app to connect
    networks:
      - project-network

networks:
  project-network:
    driver: bridge